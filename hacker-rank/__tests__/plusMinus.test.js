const plusMinus = require('../plusMinus');

describe('plusMinus', () => {
	test('prints the ratio of positive, negative and zero numbers in the array', () => {
		expect(plusMinus([-4, 3, -9, 0, 4, 1])).toEqual();
	});
});
